<h1>YouTube Downloader</h1>

I have no clue if this is allowed or not, but here it is.

Ensure you have the required dependencies: `pip install -r requirements.txt`<break>
Use <strong>config.ini</strong> to specify:
<ul>
    <li> the image location of your ffmpeg.exe (required) </li>
    <li> the desired output folder (optional) </li>
</ul>